pipeline {
    agent any
  
    stages {
        stage('aws') {
            agent{
                docker {
                    image 'amazon/aws-cli'
                    reuseNode true
                    args "--entrypoint=''"
                }
            }
            steps {
                sh 'aws --version'
            }
        }
    }
}