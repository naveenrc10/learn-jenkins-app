pipeline {
    agent any
  
    stages {
        stage('aws') {
            agent{
                docker {
                    image 'awscli:latest'
                    reuseNode true
                }
            }
            steps {
                sh 'aws --version'
            }
        }
    }
}